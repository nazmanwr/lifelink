/* Copy this file to firebase-config.js and paste in your own project's values.
   Firebase web config values are public by design — security comes from
   Firestore rules, not from hiding these keys. Still, keep firebase-config.js
   out of git if your repo is public (it is already in .gitignore). */
window.FIREBASE_CONFIG = {
  apiKey: 'YOUR_API_KEY',
  authDomain: 'your-project.firebaseapp.com',
  projectId: 'your-project',
  storageBucket: 'your-project.appspot.com',
  messagingSenderId: '000000000000',
  appId: '1:000000000000:web:0000000000000000000000'
};
